<template xml:space="preserve">
  <t t-name="CapturePartnerImage">
    <div class="o_capture_image_modal" style="padding: 20px;">
      <div class="o_modal_content">
        <!--div class="o_modal_header" style="margin-bottom: 20px;">
          <h3>Captura de Imagem do Parceiro</h3>
          <button type="button" class="btn-close" id="btn-close">X</button>
        </div-->

        <!-- Área com Webcam e Canvas lado a lado -->
        <div style="display: flex; flex-direction: row; justify-content: center; gap: 40px; margin-bottom: 20px;">
          
          <!-- Bloco da Webcam -->
          <div style="display: flex; flex-direction: column; align-items: center;">
            <h4>📷 Webcam</h4>
            <video t-att-id="'video'"
                   t-att-width="'320'"
                   t-att-height="'240'"
                   t-att-style="'border: 1px solid #ccc; border-radius: 8px;'"
                   t-att-autoplay="'autoplay'"
                   t-att-playsinline="'playsinline'">
            </video>
          </div>

          <!-- Bloco da Imagem Capturada -->
          <div style="display: flex; flex-direction: column; align-items: center;">
            <h4>🖼️ Imagem Capturada</h4>
            <canvas t-att-id="'canvas'"
                    t-att-width="'320'"
                    t-att-height="'240'"
                    t-att-style="'border: 1px solid #ccc; border-radius: 8px;'">
            </canvas>
          </div>

        </div>

        <!-- Botões -->
        <div class="o_modal_footer" style="display: flex; justify-content: center; gap: 20px;">
          <button type="button" class="btn btn-primary" id="btn-click">
            <i class="fa fa-camera"></i> Capturar
          </button>
          <button type="button" class="btn btn-secondary" id="btn-close">
            <i class="fa fa-times text-danger"></i> Fechar
          </button>
        </div>
      </div>
    </div>
  </t>
</template>